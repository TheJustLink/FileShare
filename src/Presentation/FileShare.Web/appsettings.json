{
  "Logging": {
    "LogLevel": {
      "Default": "Information",
      "Microsoft.AspNetCore": "Warning"
    }
  },

  "AllowedHosts": "*",

  "Minio": {
    "Uri": "http://${MINIO_ROOT_USER}:${MINIO_ROOT_PASSWORD}@database:9000",
    "FilesBucket": "files"
  }
}